---
- hosts: localhost

  # Hacky workaround to deal with git repo file permissions being read only
  pre_tasks:
    - command: chmod -R u+w /tmp/kitchen/sudo.sudo-watch-docker/.git

  roles:
    - sudo.sudo-watch-docker